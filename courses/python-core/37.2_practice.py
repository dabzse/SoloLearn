## Modules Part1
import random
random.seed(int(input())) # please don't touch this lane
        ## I think that should be "LINE" not "LANE"

# generate the random values for every dice
dice1 = random.randint(1, 6)
dice2 = random.randint(1, 6)

print(dice1)
print(dice2)