// pro

let score1 = parseInt(readLine(), 10);
let score2 = parseInt(readLine(), 10);
let score3 = parseInt(readLine(), 10);


// complete the function
function average() {
    return (score1 + score2 + score3) / 3;
}

console.log(average(score1, score2, score3));