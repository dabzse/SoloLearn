// pro

#include <iostream>
using namespace std;

// define the bot() function here

void bot(string name) {
    cout << "Welcome, " << name << "!" << endl;
}

int main() {
    string name;
    cin >> name;
    bot(name);
}
